$kind: http-request
name: "Evidencia legítima: apertura"
url: "{{base_url}}/api/v1/enrollments/{{enrollment_id}}/progress"
method: POST
headers:
  Content-Type: application/json
  Authorization: Bearer {{session_token}}
  Idempotency-Key: "{{idem_key}}"
body:
  type: text
  content: |-
    {
      "resource_stable_id": "{{text_stable_id}}",
      "kind": "open"
    }
scripts:
  - type: beforeRequest
    code: pm.collectionVariables.set('idem_key', 'pm-' + Date.now() + '-' + Math.random().toString(36).slice(2));
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('202', () => pm.response.to.have.status(202));
      pm.test('aceptada', () => pm.expect(pm.response.json().accepted).to.be.true);
    language: text/javascript
order: 2000
